version: "3.8"
services:
  db:
    image: docker.io/library/postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bike_sharing_dev
    ports:
      - "5432:5432"
